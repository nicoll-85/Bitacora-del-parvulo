include includes/head.pug
body
	.row
		.col-12
			+banner()
	.row
		.container
			.col-12
				+button({'buttonTitle': 'Mi botón','showIcon': true, 'iconName': 'Synthesis'}).button--secondary
			.col-12
				+button({'buttonTitle': 'Mi botón primario'}).button--primary
			.col-12
				+button({'buttonTitle': 'Ghost button'}).button--ghost
			.col-12.mt-5
				+card
			.col-12.mt-5
				+label.label__default
				+label({'labelTitle': 'Making Decisions'}).label__default
				+label({'labelTitle': 'Planning & Organinzing'}).label__default
	+footer()
